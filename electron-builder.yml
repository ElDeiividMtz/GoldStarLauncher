appId: 'goldstarlauncher'
productName: 'GoldStar Launcher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2026 GoldStar Studio'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs}'

extraResources:
  - 'libraries'

# Windows Configuration
win:
  target:
    - target: 'nsis'
      arch: 'x64'
    - target: 'zip'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'GoldStar Studio'
  vendor: 'GoldStar Studio'
  synopsis: 'GoldStar Minecraft Launcher'
  description: 'Launcher oficial de GoldStar Studio. Todos los mods, configuraciones y actualizaciones se manejan automaticamente.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'